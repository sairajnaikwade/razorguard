{
  "name": "razorguard",
  "version": "1.0.0",
  "description": "RazorGuard — AI Payment Fraud Detector & Investigation Platform",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build": "npm run build --prefix frontend",
    "backend:dev": "cd backend && .\\venv\\Scripts\\uvicorn app.main:app --reload",
    "test:backend": "cd backend && .\\venv\\Scripts\\python -m pytest tests/ -v"
  }
}
